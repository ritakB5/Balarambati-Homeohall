module.exports = {
  // Use single quotes instead of double quotes
  singleQuote: true,
  
  // Use spaces instead of tabs
  useTabs: false,
  tabWidth: 2,
  
  // Add a trailing comma wherever possible (reduces git diffs)
  trailingComma: 'es5',
  
  // Print semicolons at the end of statements
  semi: true,
  
  // Line length before Prettier will wrap code
  printWidth: 100,
  
  // Use parentheses around arrow function parameters only when needed
  arrowParens: 'avoid',
  
  // Include parentheses around a sole arrow function parameter
  bracketSpacing: true,
  
  // Put the > of a multi-line JSX element at the end of the last line
  jsxBracketSameLine: false,
  
  // Use single quotes in JSX
  jsxSingleQuote: true,
  
  // Format code on save (editor-specific, not Prettier config)
  // formatOnSave: true,
  
  // Use .gitattributes to handle line endings
  endOfLine: 'lf',
  
  // Format embedded code if Prettier can identify it
  embeddedLanguageFormatting: 'auto',
  
  // HTML whitespace sensitivity
  htmlWhitespaceSensitivity: 'css',
  
  // Control whether Prettier formats quoted code embedded in the file
  proseWrap: 'preserve',
  
  // Controls the display of spaces in Vue template interpolation
  vueIndentScriptAndStyle: false,
  
  // Control whether to add whitespace before function parentheses
  arrowParens: 'always',
  
  // Control whether to add spaces inside object literals
  bracketSameLine: false,
  
  // Control whether to add spaces inside single-line objects
  bracketSpacing: true,
  
  // Control where to put the closing `>` of HTML elements that can be either on a single line or multiple lines
  bracketSameLine: false,
  
  // Control whether to indent script and style tags in Vue files
  vueIndentScriptAndStyle: false,
  
  // Control whether to add a space before function parentheses
  arrowParens: 'always',
  
  // Control whether to add spaces inside object literals
  bracketSpacing: true,
  
  // Control where to put the closing `>` of HTML elements that can be either on a single line or multiple lines
  bracketSameLine: false,
  
  // Control whether to indent script and style tags in Vue files
  vueIndentScriptAndStyle: false,
  
  // Control whether to add a space before function parentheses
  arrowParens: 'always',
  
  // Control whether to add spaces inside object literals
  bracketSpacing: true,
  
  // Control where to put the closing `>` of HTML elements that can be either on a single line or multiple lines
  bracketSameLine: false,
  
  // Control whether to indent script and style tags in Vue files
  vueIndentScriptAndStyle: false,
};
